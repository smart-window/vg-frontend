/**
 * Enum-style constants used to control 'type' prop of VgInput
 * @category Modules - Web
 * @subcategory VgInput
 * @namespace InputTypes
 */

const ADDRESS ='address'
const BOOLEAN = 'boolean'
const DATE = 'date'
const NUMBER = 'number'
const PHONE = 'tel'
const PRIVATE = 'private'
const SELECT = 'select'
const TEXT = 'text'
const TEXTAREA = 'textarea'

export default {
  ADDRESS,
  BOOLEAN,
  DATE,
  NUMBER,
  PHONE,
  PRIVATE,
  SELECT,
  TEXT,
  TEXTAREA,
}
